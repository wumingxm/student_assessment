<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.student_assessment.dao.ModelDao">
	<select id="selectModel2" resultType="Model">
		select * from model 
	</select>
	
	<select id="selectModelById" resultType="Model" parameterType="integer">
		select * from model  where pid=#{value}
	</select>
</mapper>